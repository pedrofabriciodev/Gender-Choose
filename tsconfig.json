{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".", // Define a raiz do projeto como base para os caminhos
    "paths": {
      "@/*": ["src/*"] // Configuração do alias para TypeScript
    }
  }
}
